# AI-Assisted TRIZ Solver

這是一個基於 **TRIZ (發明問題解決理論)** 的 AI 輔助工程解決方案引擎。它引導工程師將模糊的技術問題轉化為標準矛盾參數，並提供創新發明原則建議。

> **🌐 線上版本 (Online Demo)**
> 本專案支援 GitHub Pages，可直接在瀏覽器使用：
> [前往 GitHub Pages 網站](https://<your-username>.github.io/TRIZ-Solver/) (部署後可用)

---

## 📂 專案結構 (Directory Structure)

本專案分為以下三個主要區塊：

1.  **guides/** - **文件與手冊**
    *   📖 [Deployment_SOP.md](guides/Deployment_SOP.md): 詳細的 GitHub Pages 部署教學。
    *   ⚙️ [Technical_Specs.md](guides/Technical_Specs.md): 系統架構與技術規格書 (TRD)。
2.  **docs/** - **GitHub Pages 部署版本 (Source of Truth)**
    *   包含最新的移動端響應式 UI (v1.1)。
    *   **開發建議**：請先修改此處的 HTML/CSS/JS，再同步至其他目錄。
3.  **static/** - **Python Web Server 靜態資源**
    *   由 `app.py` 調用，內容應與 `docs/` 保持同步。
4.  **根目錄 (Root)** - **本地靜態版與 Python 原始碼**
    *   `index.html`: 本地直接開啟的靜態入口。
    *   `app.py`: FastAPI 伺服器入口。
    *   `main.py`: CLI 版本入口。

---

## 🚀 如何使用 (Usage)

### 模式 A：網頁瀏覽器版 (推薦 for Users)
直接訪問 GitHub Pages 網址。

### 模式 B：本地 Python 服務 (推薦 for Developers)
1.  安裝依賴：`pip install -r requirements.txt`
2.  啟動服務：`python app.py` (會自動開啟 `http://127.0.0.1:8000/static/index.html`)

---

## 🛠️ 維護與同步指南 (v1.1 更新)

為了確保不同進入點（GitHub Pages, Local, Python App）的體驗一致，請遵循：

1.  **修改 UI**：統一修改 `docs/` 下的 `index.html`、`style.css`、`main.js`。
2.  **同步檔案**：修改後執行同步，將 `docs/` 的內容覆蓋至根目錄與 `static/` 資料夾。
3.  **快取清理**：HTML 中的 CSS 引用帶有 `?v=1.1` 標記，若有重大更新請增加版本號以強制手機端刷新。

---

<footer style="text-align: center; margin-top: 50px; color: #888;">
    Developed by Wesley Chang @2026
</footer>
