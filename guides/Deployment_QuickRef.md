# GitHub Pages 部署快速參考

## 📁 檔案結構

```
TRIZ/
├── docs/                      ← GitHub Pages 來源資料夾
│   ├── index.html            ← 主頁面
│   ├── .nojekyll             ← 停用 Jekyll
│   ├── data/                 ← TRIZ 知識庫
│   │   ├── parameters.json   (39 個工程參數)
│   │   ├── principles.json   (40 個發明原則)
│   │   └── matrix.json       (矛盾矩陣)
│   ├── css/
│   │   └── style.css
│   └── js/
│       ├── engine.js         ← JavaScript 引擎 (移植自 Python)
│       └── main.js           ← 網頁邏輯
├── DEPLOYMENT_SOP.md         ← 詳細部署教學
└── README.md                 ← 專案說明
```

---

## 🚀 快速部署步驟

### 1. 建立 GitHub Repository
- 登入 GitHub
- 點擊 **New repository**
- 名稱：`TRIZ-Solver`
- 設定為 **Public**
- 點擊 **Create repository**

### 2. 上傳檔案
- 點擊 **uploading an existing file**
- 將 `docs/` 資料夾**內的所有檔案**拖曳上傳
- ⚠️ 注意：上傳 `docs` 裡面的內容，不是 `docs` 資料夾本身
- Commit message: `Initial commit`

### 3. 啟用 GitHub Pages
- Settings → Pages
- Source: `main` / `/ (root)`
- 點擊 **Save**

### 4. 訪問網站
- 網址：`https://<username>.github.io/TRIZ-Solver/`
- 等待 1-2 分鐘建置完成

---

## ✅ 驗證清單

部署後請確認：

- [ ] 網頁正常顯示（無 404 錯誤）
- [ ] 左側 Console 顯示 "Engine ready"
- [ ] Step 1 可以輸入系統名稱並進入 Step 2
- [ ] Step 2 可以解析參數（點擊 🔍 有反應）
- [ ] Step 3 可以顯示發明原則卡片

---

## 🔧 常見問題

### Q: 網頁顯示 404
**A**: 確認檔案結構正確，`index.html` 應在 Repository 根目錄

### Q: 樣式跑掉
**A**: 確認 `css/` 和 `js/` 資料夾都有上傳

### Q: 點擊按鈕沒反應
**A**: 
1. 按 F12 打開開發者工具
2. 查看 Console 錯誤訊息
3. 確認 `data/` 資料夾內的 JSON 檔案都有上傳

---

## 📖 詳細說明

請參閱 [DEPLOYMENT_SOP.md](DEPLOYMENT_SOP.md) 取得完整的圖文教學。

---

## 🧪 本地測試

在部署前，可先在本機測試：

```powershell
cd docs
python -m http.server 8080
```

然後訪問 `http://localhost:8080`
